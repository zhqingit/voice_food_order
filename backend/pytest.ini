[pytest]
pythonpath = .
filterwarnings =
    # python-jose uses datetime.utcnow() internally; keep test output clean on Python 3.12+
    ignore:datetime\.datetime\.utcnow\(\) is deprecated.*:DeprecationWarning:jose\.jwt
