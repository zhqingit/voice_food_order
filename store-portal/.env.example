# Optional base URL for the FastAPI backend.
#
# Dev default: leave unset so Vite proxies `/store/*` to `http://localhost:8000`
# and forces the Host header to `store-api.local` for backend host-policy checks.
#
# Prod example:
# VITE_STORE_API_BASE_URL=https://store-api.example.com
