# Optional base URL for the FastAPI backend.
#
# Dev default: leave unset so Vite proxies `/admin/*` to `http://localhost:8000`
# and forces the Host header to `admin-api.local` for backend host-policy checks.
#
# Prod example:
# VITE_ADMIN_API_BASE_URL=https://admin-api.example.com
